# Zero-Shot Baseline Report (20260901_180158)

## High-Level Metrics
- **Total SQL Turns Evaluated**: 75
- **Passed Turns**: 30
- **Baseline Accuracy**: 40.00%
- **Average Latency**: 20.26s

## Failures
### conv_1 Turn 1
**Query**: Show me all active farmers in Solapur district.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, name
FROM farmer
WHERE district = 'Solapur' AND is_active = TRUE;
```
**Gold SQL**:
```sql
SELECT * FROM farmer WHERE is_active = true AND district = 'Solapur'
```

### conv_1 Turn 2
**Query**: Filter that to those who registered after Jan 1, 2023.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, name
FROM farmer
WHERE district = 'Solapur' AND is_active = TRUE AND registered_on > '2023-01-01';
```
**Gold SQL**:
```sql
SELECT * FROM farmer WHERE is_active = true AND district = 'Solapur' AND registered_on > '2023-01-01'
```

### conv_1 Turn 3
**Query**: Wait, I meant Sangli district, not Solapur.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, name
FROM farmer
WHERE district = 'Sangli' AND is_active = TRUE AND registered_on > '2023-01-01';
```
**Gold SQL**:
```sql
SELECT * FROM farmer WHERE is_active = true AND district = 'Sangli' AND registered_on > '2023-01-01'
```

### conv_3 Turn 2
**Query**: Only show soil moisture readings.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT *
FROM sensor_reading
WHERE plot_id = '5' AND reading_type = 'soil_moisture';
```
**Gold SQL**:
```sql
SELECT * FROM sensor_reading WHERE plot_id = 5 AND reading_type = 'soil_moisture' ORDER BY recorded_at DESC LIMIT 1
```

### conv_4 Turn 2
**Query**: Which plots do they belong to?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT DISTINCT plot_id
FROM advisory
WHERE severity = 'critical';
```
**Gold SQL**:
```sql
SELECT p.* FROM advisory a JOIN plot p ON a.plot_id = p.id WHERE a.severity = 'critical'
```

### conv_4 Turn 3
**Query**: Who are the farmers for those plots?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT DISTINCT f.name
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
WHERE p.id IN (
    SELECT DISTINCT plot_id
    FROM advisory
    WHERE severity = 'critical'
);
```
**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN advisory a ON p.id = a.plot_id WHERE a.severity = 'critical'
```

### conv_5 Turn 2
**Query**: How many field visits did each of them make?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT agent_id, COUNT(*) AS visit_count
FROM field_visit
GROUP BY agent_id;
```
**Gold SQL**:
```sql
SELECT a.id, a.name, COUNT(v.id) FROM field_agent a LEFT JOIN field_visit v ON a.id = v.agent_id GROUP BY a.id, a.name
```

### conv_5 Turn 3
**Query**: Only show agents with more than 10 visits.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT agent_id, COUNT(*) AS visit_count
FROM field_visit
GROUP BY agent_id
HAVING COUNT(*) > 10;
```
**Gold SQL**:
```sql
SELECT a.id, a.name, COUNT(v.id) AS visit_count FROM field_agent a LEFT JOIN field_visit v ON a.id = v.agent_id GROUP BY a.id, a.name HAVING COUNT(v.id) > 10
```

### conv_5 Turn 4
**Query**: Which of those visits were successful?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, visited_at, outcome, notes
FROM field_visit
WHERE outcome = 'successful';
```
**Gold SQL**:
```sql
SELECT v.* FROM field_agent a JOIN field_visit v ON a.id = v.agent_id WHERE a.id IN (SELECT a2.id FROM field_agent a2 JOIN field_visit v2 ON a2.id = v2.agent_id GROUP BY a2.id HAVING COUNT(v2.id) > 10) AND v.outcome = 'completed'
```

### conv_7 Turn 1
**Query**: Find all crop cycles that have been harvested.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT *
FROM crop_cycle
WHERE status = 'harvested';
```
**Gold SQL**:
```sql
SELECT * FROM crop_cycle WHERE status = 'H'
```

### conv_7 Turn 2
**Query**: What is the average actual yield for these?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT AVG(actual_yield) AS average_actual_yield
FROM crop_cycle
WHERE status = 'harvested';
```
**Gold SQL**:
```sql
SELECT AVG(actual_yield) FROM crop_cycle WHERE status = 'H'
```

### conv_7 Turn 3
**Query**: Group the average yield by crop type.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT crop, AVG(actual_yield) AS average_yield
FROM crop_cycle
WHERE status = 'harvested'
GROUP BY crop;
```
**Gold SQL**:
```sql
SELECT crop, AVG(actual_yield) FROM crop_cycle WHERE status = 'H' GROUP BY crop
```

### conv_9 Turn 1
**Query**: List all farmers.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, name, district, state, registered_on, is_active FROM farmer;
```
**Gold SQL**:
```sql
SELECT * FROM farmer
```

### conv_9 Turn 2
**Query**: How much total land area does each farmer have?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) AS total_land_area
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
GROUP BY f.id, f.name;
```
**Gold SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) FROM farmer f LEFT JOIN plot p ON f.id = p.farmer_id GROUP BY f.id, f.name
```

### conv_9 Turn 3
**Query**: Show only farmers in Maharashtra state.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, name, district, state, registered_on, is_active 
FROM farmer 
WHERE state = 'Maharashtra';
```
**Gold SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) FROM farmer f LEFT JOIN plot p ON f.id = p.farmer_id WHERE f.state = 'Maharashtra' GROUP BY f.id, f.name
```

### conv_10 Turn 1
**Query**: Find the average sensor reading value for temperature.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT AVG(value) AS average_temperature
FROM sensor_reading
WHERE reading_type = 'temperature';
```
**Gold SQL**:
```sql
SELECT AVG(value) FROM sensor_reading WHERE reading_type = 'temperature' AND value NOT IN (-273.0, 999.9)
```

### conv_10 Turn 2
**Query**: Instead of average, give me the maximum value.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT MAX(value) AS max_temperature
FROM sensor_reading
WHERE reading_type = 'temperature';
```
**Gold SQL**:
```sql
SELECT MAX(value) FROM sensor_reading WHERE reading_type = 'temperature' AND value NOT IN (-273.0, 999.9)
```

### conv_10 Turn 3
**Query**: What is the maximum for humidity?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT MAX(value) AS max_humidity
FROM sensor_reading
WHERE reading_type = 'humidity';
```
**Gold SQL**:
```sql
SELECT MAX(value) FROM sensor_reading WHERE reading_type = 'humidity' AND value NOT IN (-273.0, 999.9)
```

### conv_11 Turn 2
**Query**: Which unique agents made these visits?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT DISTINCT agent_id
FROM field_visit
WHERE outcome = 'rescheduled';
```
**Gold SQL**:
```sql
SELECT DISTINCT a.* FROM field_agent a JOIN field_visit v ON a.id = v.agent_id WHERE v.outcome = 'rescheduled'
```

### conv_12 Turn 2
**Query**: Add sprinkler irrigation to that list.
**Reason**: DB Execution Error: Database Execution Error: permission denied for table plot
**Generated SQL**:
```sql
UPDATE plot
SET irrigation_type = 'sprinkler'
WHERE irrigation_type = 'drip';
```
**Gold SQL**:
```sql
SELECT * FROM plot WHERE irrigation_type IN ('drip', 'sprinkler')
```

### conv_12 Turn 3
**Query**: What's the total area of these plots?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT SUM(area_hectares) AS total_area_hectares
FROM plot;
```
**Gold SQL**:
```sql
SELECT SUM(area_hectares) FROM plot WHERE irrigation_type IN ('drip', 'sprinkler')
```

### conv_13 Turn 1
**Query**: List all farmers who have inactive status.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT f.id, f.name, f.district, f.state, f.registered_on, f.is_active
FROM farmer f
WHERE f.is_active = FALSE;
```
**Gold SQL**:
```sql
SELECT * FROM farmer WHERE is_active = false
```

### conv_13 Turn 2
**Query**: Did any of them have crop cycles in the kharif season?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT f.id, f.name, f.district, f.state, f.registered_on, f.is_active
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
JOIN crop_cycle cc ON p.id = cc.plot_id
WHERE f.is_active = FALSE AND cc.season = 'kharif';
```
**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN crop_cycle c ON p.id = c.plot_id WHERE f.is_active = false AND c.season = 'kharif'
```

### conv_15 Turn 1
**Query**: Show the top 5 plots with the largest area.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT id, farmer_id, area_hectares, soil_type, district, irrigation_type
FROM plot
ORDER BY area_hectares DESC
LIMIT 5;
```
**Gold SQL**:
```sql
SELECT * FROM plot ORDER BY area_hectares DESC LIMIT 5
```

### conv_15 Turn 2
**Query**: Who are the farmers for these plots?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT f.id, f.name
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
ORDER BY p.area_hectares DESC
LIMIT 5;
```
**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id ORDER BY p.area_hectares DESC LIMIT 5
```

### conv_15 Turn 3
**Query**: When did they register?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT f.registered_on
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
ORDER BY p.area_hectares DESC
LIMIT 5;
```
**Gold SQL**:
```sql
SELECT f.name, f.registered_on FROM farmer f JOIN plot p ON f.id = p.farmer_id ORDER BY p.area_hectares DESC LIMIT 5
```

### conv_16 Turn 1
**Query**: Rank the farmers in each district by their total plot area, showing the top 3 per district.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT 
    f.district, 
    f.name, 
    SUM(p.area_hectares) AS total_area_hectares
FROM 
    farmer f
JOIN 
    plot p ON f.id = p.farmer_id
GROUP BY 
    f.district, 
    f.name
ORDER BY 
    f.district, 
    total_area_hectares DESC
LIMIT 3;
```
**Gold SQL**:
```sql
WITH FarmerArea AS (SELECT f.id, f.name, f.district, SUM(p.area_hectares) as total_area FROM farmer f JOIN plot p ON f.id = p.farmer_id GROUP BY f.id, f.name, f.district) SELECT * FROM (SELECT name, district, total_area, RANK() OVER(PARTITION BY district ORDER BY total_area DESC) as rnk FROM FarmerArea) sub WHERE rnk <= 3
```

### conv_16 Turn 2
**Query**: Actually, rank them by expected yield instead of plot area.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT 
    f.district, 
    f.name, 
    SUM(cc.expected_yield) AS total_expected_yield
FROM 
    farmer f
JOIN 
    plot p ON f.id = p.farmer_id
JOIN 
    crop_cycle cc ON p.id = cc.plot_id
GROUP BY 
    f.district, 
    f.name
ORDER BY 
    f.district, 
    total_expected_yield DESC
LIMIT 3;
```
**Gold SQL**:
```sql
WITH FarmerYield AS (SELECT f.id, f.name, f.district, SUM(c.expected_yield) as total_yield FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN crop_cycle c ON p.id = c.plot_id GROUP BY f.id, f.name, f.district) SELECT * FROM (SELECT name, district, total_yield, RANK() OVER(PARTITION BY district ORDER BY total_yield DESC) as rnk FROM FarmerYield) sub WHERE rnk <= 3
```

### conv_17 Turn 1
**Query**: Show me the average soil moisture reading per month across all plots.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT 
    DATE_TRUNC('month', sr.recorded_at) AS month,
    AVG(sr.value) AS average_soil_moisture
FROM 
    sensor_reading sr
WHERE 
    sr.reading_type = 'soil_moisture'
GROUP BY 
    month
ORDER BY 
    month;
```
**Gold SQL**:
```sql
SELECT DATE_TRUNC('month', recorded_at) as month, AVG(value) FROM sensor_reading WHERE reading_type = 'soil_moisture' AND value NOT IN (-273.0, 999.9) GROUP BY DATE_TRUNC('month', recorded_at)
```

### conv_17 Turn 2
**Query**: Filter that to just plot 15.
**Reason**: DB Execution Error: Database Execution Error: invalid input syntax for type integer: "plot_15_id"
**Generated SQL**:
```sql
SELECT 
    DATE_TRUNC('month', sr.recorded_at) AS month,
    AVG(sr.value) AS average_soil_moisture
FROM 
    sensor_reading sr
WHERE 
    sr.reading_type = 'soil_moisture'
    AND sr.plot_id = 'plot_15_id'
GROUP BY 
    month
ORDER BY 
    month;
```
**Gold SQL**:
```sql
SELECT DATE_TRUNC('month', recorded_at) as month, AVG(value) FROM sensor_reading WHERE reading_type = 'soil_moisture' AND value NOT IN (-273.0, 999.9) AND plot_id = 15 GROUP BY DATE_TRUNC('month', recorded_at)
```

### conv_18 Turn 2
**Query**: I meant the actual yield, grouped by season.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT season, AVG(actual_yield) AS average_yield
FROM crop_cycle
GROUP BY season;
```
**Gold SQL**:
```sql
SELECT season, SUM(actual_yield) FROM crop_cycle WHERE crop = 'chickpea' GROUP BY season
```

### conv_18 Turn 3
**Query**: Also show the count of crop cycles per season.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT season, AVG(actual_yield) AS average_yield, COUNT(*) AS cycle_count
FROM crop_cycle
GROUP BY season;
```
**Gold SQL**:
```sql
SELECT season, SUM(actual_yield), COUNT(*) FROM crop_cycle WHERE crop = 'chickpea' GROUP BY season
```

### conv_20 Turn 2
**Query**: The unique ones with unacknowledged urgent advisories.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT DISTINCT a.plot_id
FROM advisory a
WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL;
```
**Gold SQL**:
```sql
SELECT DISTINCT p.* FROM plot p JOIN advisory a ON p.id = a.plot_id WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL
```

### conv_20 Turn 3
**Query**: Who are the farmers for those plots?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT DISTINCT f.id, f.name
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
WHERE p.id IN (
    SELECT DISTINCT a.plot_id
    FROM advisory a
    WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL
);
```
**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN advisory a ON p.id = a.plot_id WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL
```

### conv_21 Turn 2
**Query**: How many active crop cycles do those farmers have?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT COUNT(*) 
FROM farmer 
JOIN plot ON farmer.id = plot.farmer_id 
JOIN crop_cycle ON plot.id = crop_cycle.plot_id 
WHERE farmer.is_active = TRUE AND crop_cycle.status = 'active';
```
**Gold SQL**:
```sql
SELECT COUNT(*) FROM crop_cycle c JOIN plot p ON c.plot_id = p.id JOIN farmer f ON p.farmer_id = f.id WHERE f.district = 'Dharwad' AND c.status = 'A'
```

### conv_21 Turn 3
**Query**: What is the total expected yield for those active cycles?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT SUM(expected_yield) 
FROM farmer 
JOIN plot ON farmer.id = plot.farmer_id 
JOIN crop_cycle ON plot.id = crop_cycle.plot_id 
WHERE farmer.is_active = TRUE AND crop_cycle.status = 'active';
```
**Gold SQL**:
```sql
SELECT SUM(c.expected_yield) FROM crop_cycle c JOIN plot p ON c.plot_id = p.id JOIN farmer f ON p.farmer_id = f.id WHERE f.district = 'Dharwad' AND c.status = 'A'
```

### conv_22 Turn 1
**Query**: What is the average temperature reading across all plots?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT AVG(value) AS average_temperature
FROM sensor_reading
WHERE reading_type = 'temperature';
```
**Gold SQL**:
```sql
SELECT AVG(value) FROM sensor_reading WHERE reading_type = 'temperature' AND value NOT IN (-273.0, 999.9)
```

### conv_22 Turn 2
**Query**: Break it down by plot.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT p.id AS plot_id, AVG(sr.value) AS average_temperature
FROM plot p
JOIN sensor_reading sr ON p.id = sr.plot_id
WHERE sr.reading_type = 'temperature'
GROUP BY p.id;
```
**Gold SQL**:
```sql
SELECT plot_id, AVG(value) FROM sensor_reading WHERE reading_type = 'temperature' AND value NOT IN (-273.0, 999.9) GROUP BY plot_id
```

### conv_22 Turn 3
**Query**: Only show plots with average temperature above 30.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT p.id AS plot_id, AVG(sr.value) AS average_temperature
FROM plot p
JOIN sensor_reading sr ON p.id = sr.plot_id
WHERE sr.reading_type = 'temperature'
GROUP BY p.id
HAVING AVG(sr.value) > 30;
```
**Gold SQL**:
```sql
SELECT plot_id, AVG(value) as avg_temp FROM sensor_reading WHERE reading_type = 'temperature' AND value NOT IN (-273.0, 999.9) GROUP BY plot_id HAVING AVG(value) > 30
```

### conv_25 Turn 2
**Query**: Which farmers own those plots?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT f.id, f.name
FROM farmer f
JOIN plot p ON f.id = p.farmer_id
WHERE p.area_hectares > (SELECT AVG(area_hectares) FROM plot);
```
**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id WHERE p.area_hectares > (SELECT AVG(area_hectares) FROM plot)
```

### conv_26 Turn 2
**Query**: How many plots fall into each category?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT 
    size_category, 
    COUNT(*) AS plot_count
FROM (
    SELECT 
        CASE 
            WHEN area_hectares < 2 THEN 'small'
            WHEN area_hectares BETWEEN 2 AND 5 THEN 'medium'
            ELSE 'large'
        END AS size_category
    FROM 
        plot
) AS categorized_plots
GROUP BY 
    size_category;
```
**Gold SQL**:
```sql
SELECT CASE WHEN area_hectares < 2 THEN 'small' WHEN area_hectares BETWEEN 2 AND 5 THEN 'medium' ELSE 'large' END AS size_category, COUNT(*) as plot_count FROM plot GROUP BY 1
```

### conv_28 Turn 1
**Query**: Show me farmers who registered more than 2 years ago.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT name
FROM farmer
WHERE registered_on <= CURRENT_DATE - INTERVAL '2 years';
```
**Gold SQL**:
```sql
SELECT * FROM farmer WHERE registered_on < CURRENT_DATE - INTERVAL '2 years'
```

### conv_28 Turn 3
**Query**: What is their average total plot area?
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT AVG(plot.area_hectares) AS average_total_plot_area
FROM farmer
JOIN plot ON farmer.id = plot.farmer_id
WHERE farmer.registered_on <= CURRENT_DATE - INTERVAL '2 years';
```
**Gold SQL**:
```sql
SELECT AVG(total_area) FROM (SELECT f.id, SUM(p.area_hectares) AS total_area FROM farmer f JOIN plot p ON f.id = p.farmer_id WHERE f.registered_on < CURRENT_DATE - INTERVAL '2 years' AND f.is_active = true GROUP BY f.id) x
```

### conv_29 Turn 1
**Query**: Show me all districts where we have either farmers or field agents.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT DISTINCT district
FROM farmer
UNION
SELECT DISTINCT district
FROM field_agent;
```
**Gold SQL**:
```sql
SELECT district FROM farmer UNION SELECT district FROM field_agent
```

### conv_29 Turn 2
**Query**: Only districts that appear in both.
**Reason**: Execution mismatch (results differ)
**Generated SQL**:
```sql
SELECT district
FROM farmer
INTERSECT
SELECT district
FROM field_agent;
```
**Gold SQL**:
```sql
SELECT district FROM farmer INTERSECT SELECT district FROM field_agent
```

