# Evaluation Report (20260902_010755)

## 1. High-Level Metrics
- **Total Conversations**: 30
- **Total Turns**: 89
- **Per-Turn Accuracy**: 74.16%
- **Full-Conversation Accuracy**: 46.67%
- **Baseline Comparison**: Zero-shot Qwen 7B (baseline: 40.0%) vs Architecture (74.16%). **Delta: +34.16%**

## 2. Performance Profiling
### Turn Metrics
- **P50 Latency (Median)**: 21.90 seconds
- **P95 Latency**: 42.20 seconds
- **Average Tokens per Turn**: 2731

### Runtime Metrics
- **Total Eval Runtime**: 2032.05 seconds
- **Average Conversation Runtime**: 67.74 seconds
- **Max Conversation Runtime**: 124.37 seconds (conv_1)
- **Min Conversation Runtime**: 35.23 seconds (conv_3)

## 3. Intent Breakdown (System-level)
| Intent | Total | Passed | Accuracy |
|---|---|---|---|
| CLARIFY | 4 | 1 | 25.00% |
| NEW | 28 | 25 | 89.29% |
| REFINE | 50 | 33 | 66.00% |
| REFUSE | 7 | 7 | 100.00% |

## 4. Category Breakdown (Assignment-level)
| Category | Total | Passed | Accuracy |
|---|---|---|---|
| ambiguous | 1 | 0 | 0.00% |
| correction | 7 | 6 | 85.71% |
| first-turn | 30 | 25 | 83.33% |
| follow-up | 43 | 27 | 62.79% |
| other | 1 | 1 | 100.00% |
| unanswerable | 7 | 7 | 100.00% |

## 5. Failure Analysis
**Total Failures**: 23 / 89

### Failure #1 — conv_4 Turn 2
**Query**: Which plots do they belong to?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT p.* FROM advisory a JOIN plot p ON a.plot_id = p.id WHERE a.severity = 'critical'
```
**Generated SQL**:
```sql
SELECT a.id, a.issued_at, a.category, a.severity, p.id AS plot_id FROM advisory AS a JOIN plot AS p ON a.plot_id = p.id WHERE a.severity ILIKE 'critical' ORDER BY a.issued_at DESC LIMIT 100
```

#### 🔍 Root Cause Diagnosis
- **Diagnosis**: [To be filled by engineer]
- **Proposed Fix**: [To be filled by engineer]

---

### Failure #2 — conv_4 Turn 3
**Query**: Who are the farmers for those plots?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN advisory a ON p.id = a.plot_id WHERE a.severity = 'critical'
```
**Generated SQL**:
```sql
SELECT f.name AS farmer_name, a.id AS advisory_id, a.issued_at, a.category, a.severity FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id JOIN advisory AS a ON p.id = a.plot_id WHERE a.severity ILIKE 'critical' ORDER BY a.issued_at DESC LIMIT 100
```

#### 🔍 Root Cause Diagnosis
- **Diagnosis**: [To be filled by engineer]
- **Proposed Fix**: [To be filled by engineer]

---

### Failure #3 — conv_5 Turn 4
**Query**: Which of those visits were successful?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT v.* FROM field_agent a JOIN field_visit v ON a.id = v.agent_id WHERE a.id IN (SELECT a2.id FROM field_agent a2 JOIN field_visit v2 ON a2.id = v2.agent_id GROUP BY a2.id HAVING COUNT(v2.id) > 10) AND v.outcome = 'completed'
```
**Generated SQL**:
```sql
SELECT fa.id, fa.name, COUNT(fv.id) AS visit_count, SUM(CASE WHEN fv.outcome = 'completed' THEN 1 ELSE 0 END) AS successful_visits FROM field_agent AS fa JOIN field_visit AS fv ON fa.id = fv.agent_id GROUP BY fa.id, fa.name HAVING COUNT(fv.id) > 10 ORDER BY visit_count DESC LIMIT 100
```

#### 🔍 Root Cause Diagnosis
- **Diagnosis**: [To be filled by engineer]
- **Proposed Fix**: [To be filled by engineer]

---

### Failure #4 — conv_9 Turn 2
**Query**: How much total land area does each farmer have?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) FROM farmer f LEFT JOIN plot p ON f.id = p.farmer_id GROUP BY f.id, f.name
```
**Generated SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) AS total_land_area FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id GROUP BY f.id, f.name ORDER BY f.id LIMIT 100
```

---

### Failure #5 — conv_9 Turn 3
**Query**: Show only farmers in Maharashtra state.

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) FROM farmer f LEFT JOIN plot p ON f.id = p.farmer_id WHERE f.state = 'Maharashtra' GROUP BY f.id, f.name
```
**Generated SQL**:
```sql
SELECT f.id, f.name, SUM(p.area_hectares) AS total_land_area FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id WHERE f.state ILIKE 'Maharashtra' GROUP BY f.id, f.name ORDER BY f.id LIMIT 100
```

---

### Failure #6 — conv_11 Turn 2
**Query**: Which unique agents made these visits?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT DISTINCT a.* FROM field_agent a JOIN field_visit v ON a.id = v.agent_id WHERE v.outcome = 'rescheduled'
```
**Generated SQL**:
```sql
SELECT DISTINCT agent_id FROM field_visit WHERE outcome ILIKE 'rescheduled' LIMIT 100
```

---

### Failure #7 — conv_13 Turn 2
**Query**: Did any of them have crop cycles in the kharif season?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN crop_cycle c ON p.id = c.plot_id WHERE f.is_active = false AND c.season = 'kharif'
```
**Generated SQL**:
```sql
SELECT f.* FROM farmer AS f WHERE f.is_active = FALSE AND EXISTS(SELECT 1 FROM crop_cycle AS c WHERE c.plot_id IN (SELECT p.id FROM plot AS p WHERE p.farmer_id = f.id) AND c.season = 'kharif') ORDER BY f.id LIMIT 100
```

---

### Failure #8 — conv_13 Turn 3
**Query**: Just show me their names.

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.name FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN crop_cycle c ON p.id = c.plot_id WHERE f.is_active = false AND c.season = 'kharif'
```
**Generated SQL**:
```sql
SELECT f.name FROM farmer AS f WHERE f.is_active = FALSE AND EXISTS(SELECT 1 FROM crop_cycle AS c WHERE c.plot_id IN (SELECT p.id FROM plot AS p WHERE p.farmer_id = f.id) AND c.season = 'kharif') ORDER BY f.id LIMIT 100
```

---

### Failure #9 — conv_14 Turn 4
**Query**: What's the best fertilizer to use?

**Expected Operation**: `CLARIFY`

**Error Reason**: Routing mismatch. Expected: CLARIFY, Got: Operation.REFINE

**Gold SQL**:
```sql
N/A
```
**Generated SQL**:
```sql
SELECT c.crop, c.season, a.category, a.severity, a.issued_at FROM crop_cycle AS c JOIN advisory AS a ON c.id = a.crop_cycle_id WHERE a.category ILIKE 'nutrient' ORDER BY a.issued_at DESC LIMIT 100
```

---

### Failure #10 — conv_15 Turn 2
**Query**: Who are the farmers for these plots?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id ORDER BY p.area_hectares DESC LIMIT 5
```
**Generated SQL**:
```sql
SELECT f.* FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id ORDER BY p.area_hectares DESC LIMIT 5
```

---

### Failure #11 — conv_16 Turn 1
**Query**: Rank the farmers in each district by their total plot area, showing the top 3 per district.

**Expected Operation**: `NEW`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
WITH FarmerArea AS (SELECT f.id, f.name, f.district, SUM(p.area_hectares) as total_area FROM farmer f JOIN plot p ON f.id = p.farmer_id GROUP BY f.id, f.name, f.district) SELECT * FROM (SELECT name, district, total_area, RANK() OVER(PARTITION BY district ORDER BY total_area DESC) as rnk FROM FarmerArea) sub WHERE rnk <= 3
```
**Generated SQL**:
```sql
SELECT f.name, f.district, SUM(p.area_hectares) AS total_area, RANK() OVER (PARTITION BY f.district ORDER BY SUM(p.area_hectares) DESC) AS rnk FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id GROUP BY f.id, f.name, f.district ORDER BY f.district, rnk LIMIT 100
```

---

### Failure #12 — conv_18 Turn 1
**Query**: Show me the yield for chickpea.

**Expected Operation**: `CLARIFY`

**Error Reason**: Routing mismatch. Expected: CLARIFY, Got: Operation.NEW

**Gold SQL**:
```sql
N/A
```
**Generated SQL**:
```sql
SELECT f.name AS farmer_name, f.district, SUM(c.actual_yield) AS total_yield FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id JOIN crop_cycle AS c ON p.id = c.plot_id WHERE c.crop ILIKE 'chickpea' GROUP BY f.name, f.district ORDER BY f.name LIMIT 100
```

---

### Failure #13 — conv_18 Turn 3
**Query**: Also show the count of crop cycles per season.

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT season, SUM(actual_yield), COUNT(*) FROM crop_cycle WHERE crop = 'chickpea' GROUP BY season
```
**Generated SQL**:
```sql
SELECT c.season, SUM(c.actual_yield) AS total_yield, COUNT(c.id) AS cycle_count FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id JOIN crop_cycle AS c ON p.id = c.plot_id WHERE c.crop ILIKE 'chickpea' GROUP BY c.season ORDER BY c.season LIMIT 100
```

---

### Failure #14 — conv_19 Turn 1
**Query**: Show me recent sensor readings for plot 50.

**Expected Operation**: `CLARIFY`

**Error Reason**: Routing mismatch. Expected: CLARIFY, Got: Operation.NEW

**Gold SQL**:
```sql
N/A
```
**Generated SQL**:
```sql
SELECT * FROM sensor_reading WHERE plot_id = 50 AND NOT value IN (-273.0, 999.9) ORDER BY recorded_at DESC LIMIT 100
```

---

### Failure #15 — conv_20 Turn 2
**Query**: The unique ones with unacknowledged urgent advisories.

**Expected Operation**: `REFINE`

**Error Reason**: Routing mismatch. Expected: REFINE, Got: Operation.CLARIFY

**Gold SQL**:
```sql
SELECT DISTINCT p.* FROM plot p JOIN advisory a ON p.id = a.plot_id WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL
```
**Generated SQL**:
```sql
N/A
```

---

### Failure #16 — conv_20 Turn 3
**Query**: Who are the farmers for those plots?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id JOIN advisory a ON p.id = a.plot_id WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL
```
**Generated SQL**:
```sql
SELECT DISTINCT f.* FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id JOIN advisory AS a ON p.id = a.plot_id WHERE a.severity = 'urgent' AND a.acknowledged_at IS NULL ORDER BY f.name LIMIT 100
```

---

### Failure #17 — conv_21 Turn 2
**Query**: How many active crop cycles do those farmers have?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT COUNT(*) FROM crop_cycle c JOIN plot p ON c.plot_id = p.id JOIN farmer f ON p.farmer_id = f.id WHERE f.district = 'Dharwad' AND c.status = 'A'
```
**Generated SQL**:
```sql
SELECT f.id, f.name, COUNT(c.id) AS active_crop_cycles FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id JOIN crop_cycle AS c ON p.id = c.plot_id WHERE f.district ILIKE 'Dharwad' AND c.status = 'A' GROUP BY f.id, f.name ORDER BY active_crop_cycles DESC LIMIT 100
```

---

### Failure #18 — conv_25 Turn 2
**Query**: Which farmers own those plots?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT f.* FROM farmer f JOIN plot p ON f.id = p.farmer_id WHERE p.area_hectares > (SELECT AVG(area_hectares) FROM plot)
```
**Generated SQL**:
```sql
SELECT f.name, p.id AS plot_id, p.area_hectares, p.soil_type, p.district, p.irrigation_type FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id WHERE p.area_hectares > (SELECT AVG(area_hectares) FROM plot) ORDER BY p.area_hectares DESC LIMIT 100
```

---

### Failure #19 — conv_26 Turn 1
**Query**: Classify all plots as small, medium, or large based on area. Small is under 2 hectares, medium is 2 to 5, and large is above 5.

**Expected Operation**: `NEW`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT id, area_hectares, CASE WHEN area_hectares < 2 THEN 'small' WHEN area_hectares BETWEEN 2 AND 5 THEN 'medium' ELSE 'large' END AS size_category FROM plot
```
**Generated SQL**:
```sql
SELECT id, farmer_id, area_hectares, CASE WHEN area_hectares < 2 THEN 'Small' WHEN area_hectares BETWEEN 2 AND 5 THEN 'Medium' ELSE 'Large' END AS size_category FROM plot ORDER BY area_hectares ASC LIMIT 100
```

---

### Failure #20 — conv_26 Turn 2
**Query**: How many plots fall into each category?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT CASE WHEN area_hectares < 2 THEN 'small' WHEN area_hectares BETWEEN 2 AND 5 THEN 'medium' ELSE 'large' END AS size_category, COUNT(*) as plot_count FROM plot GROUP BY 1
```
**Generated SQL**:
```sql
SELECT id, farmer_id, area_hectares, CASE WHEN area_hectares < 2 THEN 'Small' WHEN area_hectares BETWEEN 2 AND 5 THEN 'Medium' ELSE 'Large' END AS size_category, COUNT(*) FROM plot GROUP BY id, farmer_id, area_hectares, size_category ORDER BY size_category, area_hectares ASC LIMIT 100
```

---

### Failure #21 — conv_28 Turn 3
**Query**: What is their average total plot area?

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT AVG(total_area) FROM (SELECT f.id, SUM(p.area_hectares) AS total_area FROM farmer f JOIN plot p ON f.id = p.farmer_id WHERE f.registered_on < CURRENT_DATE - INTERVAL '2 years' AND f.is_active = true GROUP BY f.id) x
```
**Generated SQL**:
```sql
SELECT AVG(total_plot_area) AS average_total_plot_area FROM (SELECT SUM(p.area_hectares) AS total_plot_area FROM farmer AS f JOIN plot AS p ON f.id = p.farmer_id WHERE f.registered_on <= CURRENT_TIMESTAMP - INTERVAL '2 YEARS' AND f.is_active = TRUE GROUP BY f.id) AS subquery LIMIT 100
```

---

### Failure #22 — conv_29 Turn 1
**Query**: Show me all districts where we have either farmers or field agents.

**Expected Operation**: `NEW`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT district FROM farmer UNION SELECT district FROM field_agent
```
**Generated SQL**:
```sql
SELECT DISTINCT district FROM farmer UNION SELECT DISTINCT district FROM field_agent ORDER BY district LIMIT 100
```

---

### Failure #23 — conv_29 Turn 2
**Query**: Only districts that appear in both.

**Expected Operation**: `REFINE`

**Error Reason**: Execution mismatch. Row contents differ (check column aliases or values).

**Gold SQL**:
```sql
SELECT district FROM farmer INTERSECT SELECT district FROM field_agent
```
**Generated SQL**:
```sql
SELECT DISTINCT district FROM farmer INTERSECT SELECT DISTINCT district FROM field_agent ORDER BY district LIMIT 100
```

---

